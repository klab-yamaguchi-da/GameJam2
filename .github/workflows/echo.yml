name: Echo Workflow

on:
  workflow_dispatch:

jobs:
  echo:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Echo message
        run: echo "Hello from workflow_dispatch!"
